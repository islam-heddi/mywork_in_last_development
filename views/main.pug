doctype html 
meta(charset="UTF-8")
meta(name="viewport", content="width=device-width,initial-scale=1")
title My Post
link(rel="stylesheet", href="./style/main.css")
header
  h1 Mywork
  h1#userName()=name
  nav
    ul
      li
        a(href="login") Deconnect
main
  form(action='/main' method="POST")
    .post
      textarea#MyNews(name="news_post", placeholder="Wrtie something to post ....", cols="50", rows="10")
      button#post post
  #news
      each item in data
        .my_post
          h1=item.name
          p=item.publish
     
